# cdkdemo

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build get-ready-lambda` to build the library.

## Running unit tests

Run `nx test get-ready-lambda` to execute the unit tests via [Vitest](https://vitest.dev/).
