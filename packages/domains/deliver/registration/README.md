# cdkdemo

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build deliver-registration` to build the library.

## Running unit tests

Run `nx test deliver-registration` to execute the unit tests via [Vitest](https://vitest.dev/).
