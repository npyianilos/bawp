export type Student = {
  id: string;
  firstName: string;
  lastName: string;
  schoolId: string;
};

export type CreateStudentInput = {
  firstName: string;
  lastName: string;
  schoolId: string;
};
