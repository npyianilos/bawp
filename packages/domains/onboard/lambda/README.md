# cdkdemo

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build onboard-lambda` to build the library.

## Running unit tests

Run `nx test onboard-lambda` to execute the unit tests via [Vitest](https://vitest.dev/).
