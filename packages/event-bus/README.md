# cdkdemo

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build event-bus` to build the library.

## Running unit tests

Run `nx test event-bus` to execute the unit tests via [Vitest](https://vitest.dev/).
